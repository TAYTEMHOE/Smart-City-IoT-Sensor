{
  "name": "simulator",
  "version": "0.0.1",
  "description": "Standalone MQTT sensor reading simulator for the Smart City IoT dashboard",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "mqtt": "^5.16.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.2",
    "typescript": "^6.0.2"
  }
}
