# Mosquitto broker configuration for local development.
#
# No authentication is configured — this is intentional for a local dev/test
# setup only, not a production configuration. See root README for details.

listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data/

log_dest stdout
log_dest file /mosquitto/log/mosquitto.log
